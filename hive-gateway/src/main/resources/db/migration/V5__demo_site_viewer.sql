-- (Intentionally empty.) The demo viewer@site grant that used to live here moved to the dev-only seed
-- (classpath:db/seed-dev/V900__demo_seed.sql), applied only under the 'demo' profile. This file is kept as a
-- no-op so the migration version sequence is unbroken for databases that already applied up to V4.
select 1;
