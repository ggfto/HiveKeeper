# Localhost only — this is a single-tenant, on-prem server. Never bind a public interface here;
# the multi-tenant cloud topology is a separate deployment with auth + an on-prem agent.
server.address=127.0.0.1
server.port=8080
spring.application.name=hivekeeper-server
spring.mvc.async.request-timeout=120000
