{
  "name": "hivekeeper-release",
  "version": "0.0.0-managed",
  "private": true,
  "description": "Release tooling for HiveKeeper (semantic-release). Not a publishable package — versioning of the JVM modules and container images is driven from here.",
  "scripts": {
    "release": "semantic-release"
  },
  "devDependencies": {
    "semantic-release": "^25.0.7",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1"
  }
}
