# Normalize line endings: LF in the repo and working tree for text files.
* text=auto eol=lf

# Platform-specific scripts
*.bat        text eol=crlf
gradlew      text eol=lf

# Binary artifacts
*.jar        binary
