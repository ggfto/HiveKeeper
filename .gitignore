# Gradle
.gradle/
build/
# keep the wrapper jar (it is intentionally committed)
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
# Eclipse/JDT per-module output dirs
bin/

# OS / logs
*.log
.DS_Store
Thumbs.db

# tooling caches
.code-review-graph/

# Node (release tooling at the root, hive-web/website have their own)
node_modules/

# Local compose env (the committed template is .env.example)
.env

# Local secrets, keystores, and captured device data — never commit
*.pem
*.key
*.p12
*.jks
*.crt
*.csr
secrets.properties
hivekeeper-backups/
captures/local/
dev-pki/
test-restore/
test-backups/
